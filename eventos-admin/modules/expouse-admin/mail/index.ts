import { defineFeatureModule } from '../kit/featureModule'

// Mail: the drag-and-drop email builder (designer components, block model) and
// the per-event mail pages.
export default defineFeatureModule(import.meta.url, { name: 'expouse-mail' })
