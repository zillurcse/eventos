import { defineFeatureModule } from '../kit/featureModule'

// Organizer: tenant home, team management and the events list.
export default defineFeatureModule(import.meta.url, { name: 'expouse-organizer' })
